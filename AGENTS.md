Always reference CLAUDE.md for development guide.

For the rate-matching sweep module (`tools/rate_matching/`), also reference:
- `tools/rate_matching/CLAUDE.md` — module-specific development guide, architecture, testing commands
- `tools/rate_matching/README.md` — user-facing documentation, pipeline flow, config format, troubleshooting
